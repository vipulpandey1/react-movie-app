apiVersion: v1
kind: Service
metadata:
  name: react-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
  selector:
    app: react-app
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: react-app-pod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: react-app
  template:
    metadata:
      labels:
        app: react-app
    spec:
      containers:
      - name: react-container
        image: 233274203099.dkr.ecr.ap-south-1.amazonaws.com/reactapp:0f750da40a6cb36ba59cd48fd103ff6ab53e91aa
        ports:
        - containerPort: 3000  
