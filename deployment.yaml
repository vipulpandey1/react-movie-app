apiVersion: v1
kind: Service
metadata:
  name: react-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
  selector:
    app: react-app
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: react-app-pod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: react-app
  template:
    metadata:
      labels:
        app: react-app
    spec:
      containers:
      - name: react-container
        image: 233274203099.dkr.ecr.ap-south-1.amazonaws.com/reactapp:f657325bfd71a1ce944789f02f28e80c91270682
        ports:
        - containerPort: 3000  
